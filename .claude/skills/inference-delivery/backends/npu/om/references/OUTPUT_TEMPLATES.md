# 标准输出模板

本 Skill 返回给主 Skill 的结果至少应包含以下模板。各阶段按需填充对应模板。

## 首轮分析阶段

```text
=== Model Shape Of Work ===
类型:
是否存在会话状态:
推荐交付形态:
功能分支清单:

=== Functional Coverage Contract ===
目标功能范围:
本次初步适配场景:
官方入口 / demo / service API:
功能分支与入口映射:
必须验证的基本功能:
允许 fallback 的功能:
明确排除的功能:
排除是否经用户确认:
当前批次覆盖的功能:
当前批次未覆盖的功能:
每个功能分支的 OM 尝试状态:
本轮暂不支持功能及原因:
后续候选触发条件:

=== Candidate Stage Inventory ===
候选:
状态: pending / selected / in_progress / om_done / validated / integrated / failed_replan / fallback / excluded
暂缓原因: none / deferred_try / needs_evidence / non_graph_control
模块路径:
角色:
所属功能分支:
是否重计算热点:
是否依赖状态:
输入契约稳定性:
输出契约稳定性:
输入 shape / 控制值变化来源:
输出 shape 变化来源:
可选输入共存/互斥关系:
是否应合并为同一候选大图:
影响图内数学的构造/控制参数及来源: 配置 / 入口契约 / 运行时值 / 样例值
可固定 shape:
可分档动态轴或可拆分语义分区:
推荐图侧主策略（单选）: static / dymbatch / dymhw / dymdims / dymshape
已有导出入口:
推荐动作:
理由:
未进入首批的原因与下一步 OM 尝试条件:
是否覆盖主链主要计算阶段:
是否覆盖本次初步适配场景:
证据:

=== Source-Side Subgraph Contract ===
输入清单:
输入来源:
输入含义:
进入子图前的处理:
可选输入:
可选输入共存/互斥关系:
同一替换点的输入组合是否已覆盖:
影响图内数学的构造/控制参数及来源:
输入证据:

=== Output Preservation Contract ===
替换点原始输出全集:
每个输出的下游消费者:
每个输出是否进入本次适配场景内的后续计算:
导出 / OM 输出清单:
未导出输出:
未导出原因:
占位 / 伪造输出:
占位是否参与后续计算:
是否保持输出语义完整:

=== Pipeline Boundary Analysis ===
推理入口:
- ...

控制面:
- ...

状态对象:
- ...

候选重计算子图:
- ...

候选大图:
- ...

前处理 / 后处理边界:
- ...

不确定项:
- ...

需后续图侧验证的问题:
- ...

推荐首批 OM 范围:
- ...

保留原始实现的部分:
- ...
```

## 边界重评估阶段

```text
=== Boundary Revision Record ===
原候选大图:
- ...

触发原因:
- ...

保留的旧候选:
- ...

放弃的旧候选:
- ...

新增候选:
- ...

修订结果:
- ...

影响:
- ...
```

## 接回阶段

```text
=== OM Pipeline Integration Summary ===
替换点:
- ...

桥接代码位置:
- ...

主路径:
- ...

fallback 路径:
- ...

各 OM 位置与职责:
- ...

功能覆盖结果:
- ...

输出保真结果:
- ...

子图数值复查项:
- 契约一致但数值不通过的候选:
- 偏差来源初判:
- 风险输出与下游消费者:
- 端到端复查入口:
- 不可接受时回跳位置:

保留原始实现的部分:
- ...

接回后最小验证结果:
- ...

失败时回跳位置:
- ...
```
