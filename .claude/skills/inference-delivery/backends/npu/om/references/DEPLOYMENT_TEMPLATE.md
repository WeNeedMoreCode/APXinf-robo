# 部署交付模板

以下模板用于最终交付的部署说明，不绑定特定模型。若用户未指定语言，可默认输出中文。

---

# <项目名> 昇腾 NPU 部署说明

## 1. 目标

- 模型或系统名称：`<name>`
- 目标 SoC：`<soc_version>`
- 目标交付：`完整推理流程 / 服务接口 / 子图 OM + 原始控制面`

## 2. 当前部署边界

### 保留在原始代码中的部分

- `<控制面 / 状态管理 / 前处理 / 后处理 / 调度>`

### 转为 OM 的部分

- `<om_1>`：`<职责>`
- `<om_2>`：`<职责>`

### 其他执行位置

- `<torch_npu 部分>`
- `<CPU 兜底部分>`

## 3. 环境要求

- CANN：`>= 8.3.RC2`
- Python：`3.11`
- 其他关键依赖：`numpy==1.26.4`、`onnx==1.16.1`

环境准备命令由环境与 ATC 子流程产出，这里只记录最终采用的环境基线与结果，不在本模板中复述其他 Skill 的脚本入口。

## 4. 权重与模型文件

- 权重目录：`<path>`
- ONNX 产物目录：`<path>`
- OM 产物目录：`<path>`

## 5. ONNX -> OM 流程

### 5.1 导出 ONNX

说明：

- 导出入口：`<script / module>`
- 输入契约：`<shape / dtype / 语义>`

### 5.2 优化 ONNX

- 优化入口：`<由 ONNX / ATC 子流程产出的实际命令>`
- 优化产物目录：`<artifacts_dir>`

### 5.3 ATC 转换

- ATC 入口：`<由 ONNX / ATC 子流程产出的实际命令>`
- OM 输出目录：`<om_out_dir>`

## 6. 端到端推理流程

按真实业务入口描述：

1. `<入口函数 / API>`
2. `<前处理>`
3. `<OM / torch_npu / CPU 计算链路>`
4. `<后处理>`
5. `<结果聚合 / 返回>`

## 7. 当前主路径与 fallback

先写端到端主路径与端到端 fallback。若还有模块级或场景级 fallback，再单独补充，不要混在一起。

### 端到端主路径

- `<主路径描述>`

### 端到端 fallback 路径

- `<fallback 描述>`

## 8. 验证结果

### 功能验证

- `<是否跑通>`
- `<测试数据>`

### 子图级精度参考

- `<ONNX vs OM 对比结论>`

### 端到端验证结论

- `<官方样例 / 官方 demo / 官方 benchmark / 真实业务入口>`
- `<最终业务结论>`
- `<差异分析>`

### 子图级性能参考

- `<单个 OM 的 latency / throughput>`
- `<测试输入与推理模式>`

### 端到端性能

- `<端到端时延 / 吞吐>`
- `<计时入口>`
- `<是否为官方 benchmark>`

### 性能归因与调优参数

- `<瓶颈位置>`
- `<推荐调优参数>`
- `<这些结论基于子图级统计还是端到端计时>`

## 9. 已知限制

- `<当前仍未 OM 化的结构>`
- `<动态输入限制>`
- `<特殊输入场景限制>`

## 10. 后续优化方向

- `<还能继续合并的大图>`
- `<还能进一步 OM 化的剩余部分>`
- `<性能瓶颈与下一步建议>`
