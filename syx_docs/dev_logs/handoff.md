# Handoff（2026-09-23 M3 收官 10/10 → 下一轮性能优化 / 压缩用）

## 状态一句话

**引擎接入主线三步落地（2026-09-23，见 summary/2026-09-23_engine-integration.md）：① 执行器库化——ge_model_probe 6206 行沉到 crate 库面 `src/pi05/ascend_ge.rs` + `GeServe` 进程内门面（open/infer，serve_loop 与直调共用帧实现），验证梯全过（golden step0_x1 0.0%/actions 0.9% 与基线逐字同、新旧二进制冒烟 bit 恒等 0.0244/1.0% 四位全同）——子模块 e7e4cd1；② PyO3 `GeServeModel`（apxinf-py --features ascend，接口与 spool 协议逐位对齐，rust 容器 python 直调三段全通；**已知障碍 = python 宿主内 GE 库在 open 尾声杀进程**，无信号无 Traceback + TBE 子进程 8 连报错，非确定点位，纯 Rust 宿主同代码稳定——疑 GE 的 TBE 子进程管理与 python 宿主相克）——787730a；③ engine.py npu-ge 加 `APXINF_GE_TRANSPORT=inproc` 分支（_InprocClient 直调 PyO3，失败自动回落 spool 桶；npu 容器 torch_npu 8.5.1 与 9.0.1 同进程互斥，spool 仍为生产传输）。**全量 fast 回归终数锁定：10/10 + per-call model_ms P50 256.3ms = 0.682×**（跨任务 254.5-260.5 极稳，④A+B 收口）。此前战绩：M3 全量 10/10 + 0.87×（ada-gate 缺失真凶战报 2026-09-23_ada-gate-missing-root-cause.md）、GEB_SERVE_FAST 254.3ms task0 口径（serve-fast-device-direct.md）。**下一轮候选：✅ python 宿主 GE 存活障碍已终结（2026-09-24，见 summary/2026-09-24_pyhost-ge-survival-hunt.md 终局节：gdb 一栈定罪 = te fusion py_decouple static path 的 `TE_PyEval_SaveThread` 偷走 PyO3 持有的 GIL → open 返回时 pymalloc SEGV；修复两行 = open 主体包 `Python::allow_threads`（无 GIL 进入则 te 不触发 SaveThread）+ forkserver-off 默认固化——子模块 2dbffe9；验证 open 176.4s 完整返回 + infer ×3 max_diff=0.0244 与 spool 逐位同 + 250-262ms + RC=0）→ 下一步 = A3 eval 全链 inproc（9.0.1 libs 供给 npu 容器 + torch_npu 剥离=前处理 CPU 化）→ B 动态 L 定形；C 性能余项顺路（E msprof vision 65ms、D int8 probe）。** **✅ C1+C2 连打完成（2026-09-24 晚，见 summary/2026-09-24_c1-vision-glue-fix-c2-int8-probe.md）：C1 vision 65-68 回归定罪 = 纯 host glue（E2 bench 56.26 与 C2 收官 55.86 一致，设备侧洗清；真凶 d2h 3.4 + f16 解码 5.1ms）→ 零拷贝字节视图 + 三段全挂流修复（子模块 ada176d）→ vision 66→59.4，sum 246.1-246.3 = 0.654×，bit 恒等 0.0244 + task0 success 1.0；C2 int8 probe 全过（子模块 3b5d54f）：QuantMatmulDequant GE 图内单算编译✓（ND int8 直入，attr 契约 = x_quant_mode="pertoken" + transpose_weight=true [n,k]）/ 数值 W8A8 per-token（host 双参考 0.0156 ≪ 0.2774 判明）/ Q-vs-F rel=0.564% / 性能 2.41×（0.737 vs 1.790ms @ m832 k2048 n8192）——int8 全链立项条件满足（prefix -35~45ms、全链 0.44-0.55× 量级外推）。机制发现：aclmdlExecuteAsync 实为 host 阻塞到接近设备完成（bench 循环掩盖、serve 单发暴露——跨口径比数须记住）。→ 下一步 = ① C2 int8 全链立项（真权重误差谱 → prefix OM int8 重烤 → golden/LIBERO 判）或 B 动态 L 定形（原序）；spool 口径疑点（task0 264 vs 直调 246 差 18ms）全量回归时顺判；A3 inproc 保持可选。**

## ① Compact 参数（贴到 /compact 后）

聚焦保留：**C1 战果与机制**（vision 65-68 回归 = 纯 host glue：E2 bench 56.26 ≈ C2 收官 55.86 设备侧洗清，真凶 d2h 3.4 + dlconv 5.1ms；修复 = 零拷贝 f16 字节视图 + copy_h2d/d2h_async 三段全挂流单次 sync——子模块 ada176d，vision 66→59.4 sum 246=0.654×，bit 恒等 0.0244 + task0 1.0；**机制发现：aclmdlExecuteAsync 实为 host 阻塞到接近设备完成**——bench per×10 循环掩盖、serve 单发暴露）；**C2 战果**（QuantMatmulDequant 310P GE 图内全过：ND int8 直入、attr 契约 x_quant_mode="pertoken"+transpose_weight=true+[n,k]、W8A8 per-token 数值 0.564% vs f16、2.41× 性能——子模块 3b5d54f，GEB_OPTEST=qmd 可复跑）；**新锚点**（直调 sum 246.1-246.3 = 0.654×：vision 59.4 / prefix 105.7 / flow 81.0；prefix asm 2.5ms 与 flow enq 2.4ms 是剩余 glue）；**遗留疑点**（spool 口径 task0 264 vs 直调 246 差 18ms——全量回归顺判；GEB_SERVE_TIMING=1 常驻可查分段）；**关键纪律**（rand_f16 第三参是除数 U(±100/div)——probe 饱和坑三次重演；GE 裸 op 构图 attr 默认值不自动生效须显式设；transpose_x2=true 需 wt() 转置字节裸 [n,k] 会读错；pkill -f 自匹配自杀；GeServe::open env 钉死不含 GEB_DEPTH）。丢弃：E1-E5 实验中间输出、注册表 grep 过程、编译失败两轮细节。

## ② Post-compact 首句（贴到压缩后第一句）

继续 APXinf 昇腾 NPU（**✅ C1+C2 连打完成 2026-09-24（子模块 731ef5a/ada176d/3b5d54f，见 summary/2026-09-24_c1-vision-glue-fix-c2-int8-probe.md）：C1 vision 65-68 回归 = 纯 host glue 定罪（E2 设备 bench 56.26 与 C2 收官 55.86 一致；真凶 d2h 3.4 + f16 解码 5.1ms）→ 零拷贝字节视图 + async 三段挂流修复 → **直调 sum 246.1-246.3 = 0.654×**（vision 59.4/prefix 105.7/flow 81.0），bit 恒等 0.0244 + task0 success 1.0（model_ms P50 264 spool 口径）；C2 QuantMatmulDequant 单算全过（GE 图内 ND int8 直入 + x_quant_mode="pertoken" + transpose_weight=true [n,k]；W8A8 per-token 数值 0.564% vs f16；2.41× 性能 @ m832 k2048 n8192——GEB_OPTEST=qmd 复跑）**。下一轮按优先级：**1. C2 int8 全链立项（✅ 真权重 L1/L2 判决已出 2026-09-24 13:15，子模块 7e8cced：L1 权重谱 gemma_expert 0.76-0.80% / paligemma 0.87-1.04%、down_proj 个别行 11.2%；L2 真激活（golden res0，|x|max=559 outlier）× L07 gate [2048×16384]——**朴素 per-token W8A8 执行差 12.693% 不可用**（host 双参考定罪激活量化侧，W8A8 数学自身精确 0.0298）；**smoothquant α=0.5 修复成立：0.831% 且 4 矩阵泛化 0.66-1.42%**（p0gate/p17gate/a7gate=expert 侧全过，`GEB_OPTEST=qmd GEB_QMD_REAL=1 GEB_QMD_SMOOTH=1 GEB_QMD_MATRIX=p7gate|p0gate|p17gate|a7gate` 复跑；⚠ **生产形态 int8 = 1.49-1.56×（合成 2.41× 是形状效应）——立项收益按 prefix matmul 1.5-1.6× 重估，全链 ~200ms = 0.53× 量级**）→ 立项实施 = ~~smooth 因子静态校准~~ **v1 已落地（2026-09-24 13:45，int8_smooth_calib.py → /data/apxinf/pyo3_check/smooth_calib_v1.npz：36 矩阵 per-input-channel s_k，α=0.5，激活 proxy = golden 四截面包络，全库上界 163.6 与 probe 831 同向；meta 记录三条限制——单帧包络非逐层真实分布、flow 侧复用 prefix proxy、down k=16384 edge-pad；**等效性已验（err_smooth_npz.py：host 模拟器自证朴素 12.746% ≈ 设备 12.693%，npz 包络因子执行差 1.347% vs 在线单截面 1.123%——保守性代价仅 +0.2pp，npz 直接可用）**→ v2 = golden 生成器补逐层激活 dump**→ s_k 折进 int8 权重 + 算子原生 smooth_scale 输入 → prefix OM 重烤 → golden parity → task0/LIBERO 行为判（flow 10 步欧拉放大是残余行为风险）→ 全量；2. B 动态 L 定形（扩预置桶 + L 谱系探测，验收 = libero_spatial 多任务集泛化）；3. 顺手：~~spool 口径疑点~~ **已销案（2026-09-24 13:28）**——task0 复跑 P50 258.0（首跑 264、gatefast 254.3）= 跨次波动 ±3% 无回归；~~prefix asm 2.5ms 查表缓存~~ **已修（子模块 1bf4201，asm 2.5→1.5ms，sum 245.5 bit 恒等；⚠ supervisor 桶须重启吃新二进制）**；M2 eager 补 gate（技术债）**。A3. eval 全链 inproc 保持可选（abi 墙 + 宿主重写 vs ~5ms/帧收益，见 ④C）。⚠ 库面纪律照旧：GeServe::open env 钉死不含 GEB_DEPTH；GEB_SERVE_TIMING=1 常驻分段计时；rand_f16 第三参是除数；GE 裸 op 构图 attr 须显式设。验证梯照旧（golden ≤0.1%/≤2% → 冒烟 A/B bit 同 → task0 → 全量大改后）。

## ④ 性能优化方向清单（2026-09-23 收集，按优先级）

现状锚点：**eval 口径 model_ms P50 = 325.3ms**（含 serve spool 轮询）＝376ms 基线的 0.87×；纯引擎稳态 310.3ms（0.82×）。逐段（serve 实测/调用）：**vision 65-68 / prefix 127-136 / flow 100-118ms**（flow = 10 步欧拉 ≈ 10ms/步 + 每步 x d2h + styles h2d；bench 口径 flow 段内 7.6ms/步）。理论裸算力下限（mm 已 ~20 TFLOPS≈峰值）：粗估 ~200-220ms，**剩余 ~90-120ms 是 host glue + 调度 + 段间往返**——大头不在算子。

| # | 方向 | 预期收益 | 机制 | 风险/前置 |
|---|---|---|---|---|
| **A** | ✅ **已落地（2026-09-23，GEB_SERVE_FAST）**：**段间设备直连**——prefix 36 路 kv 异步 d2d（零 host 中转）+ flow x 设备驻留（ob→binds[0] d2d，只末步下载）+ 去 per-step sync（10 次→1 次） | **实测 −45~62ms**（flow 117→80、prefix 130→105-115；task0 model_ms 316→254.3） | 同流顺序链保证计算序不变——bit 恒等 | 已过验证梯：冒烟 A/B bit 同（max_diff 四位全同）+ task0 success 1.0；全量回归待下一轮 |
| **B** | ✅ **已落地（同上，与 A 同一开关）**：**styles 设备驻留**——legacy 110 槽被 10 步复用须逐步重传（1100 次 h2d/调用）；改为每步独立视图拼 2.25MB master 一次上传 ⇒ **帧内零拷贝** | 并入上项（flow −35~37ms 的主体） | styles 跨帧恒定 + DeviceBuffer::view_of 非拥有视图 | 低（同 A） |
| **C** | ✅ **已落地（2026-09-23，e7e4cd1+787730a+engine.py inproc 分支）；python 宿主障碍已终结（2026-09-24 2dbffe9，GIL 劫持定罪 + allow_threads 修复，open 完整返回 + infer 与 spool 逐位同）**：**in-process serving**——执行器沉 crate 库面（GeServe 门面，spool/直调同源帧实现）+ PyO3 GeServeModel + npu_ge.py `APXINF_GE_TRANSPORT=inproc`（回落 spool 保底）。**剩余 = A3 eval 全链 inproc**（9.0.1 libs 供给 npu 容器 + torch_npu 剥离=前处理 CPU 化） | spool 15ms 在 inproc 生效环境免 | 引擎单实现三入口（example/PyO3/spool） | 已过：golden+bit 恒等双验、rust 容器 python 直调全通（修复后 RC=0 稳定）；task0/全量回归走 spool 保底无回归 |
| **D** | **✅ 单算 probe 全过（2026-09-24，GEB_OPTEST=qmd，子模块 3b5d54f）：`QuantMatmulDequant` 在 310P GE 图内单算三连判决**——编译✓（**ND int8 权重直入**，注册表 FRACTAL_NZ 提示不拦；attr 契约 = `x_quant_mode="pertoken"` + `transpose_weight=true` [n,k]，缺 attr = task_distribute 拒且 plog 无细节）；数值✓（Q-vs-f16 执行 rel=0.564%；host 双参考 W8A8 0.0156 ≪ W8A16 0.2774 ⇒ 内部 per-token 激活量化）；性能✓（**2.41×** vs 生产转置布局 1.790ms @ m832 k2048 n8192）。**剩 = 全链立项**：真权重误差谱（Gemma outlier）→ prefix OM int8 重烤 → golden/LIBERO 行为判 | prefix **或 −35~45ms，全链 0.44-0.55× 量级** | 融合单算（激活量化在算子内部，host 激活流不变）；scale per-channel host 可精确计算 | 中：量化误差在 10 步欧拉的行为放大待 LIBERO 判；整图 TransData 税复查 |
| **E** | **✅ C1 判决+修复完成（2026-09-24，子模块 ada176d）**：vision serve 65-68 "回归" = 纯 host glue（E2 设备 bench 56.26 ≈ C2 收官 55.86，设备侧洗清；真凶 = d2h 3.4 + f16 解码 5.1ms；111 输出 dataset 重建 0.3ms 洗清）→ 零拷贝字节视图 + `copy_h2d/d2h_async` 三段挂流单次 sync → **vision 59.4 / sum 246 = 0.654×**，bit 恒等 + task0 1.0 | **已兑现 −6.6ms** | `GEB_SERVE_TIMING=1` 分段计时常驻 | 剩余小项：~~prefix asm 2.5ms~~ 已修（token 查表缓存 1bf4201，asm→1.5ms）、flow enq 2.4ms；aclmdlExecuteAsync 实为 host 阻塞语义（bench 循环掩盖/serve 单发暴露） |
| **F** | **生产化配套**（非性能项）：动态 L 方案定形（预置桶集 vs padding+mask vs input_shape_range）、OM 懒加载/共享加载（3.7GB prefix 每桶 spawn ~1min）、M2 eager 路径补 gate、NORM32 归档决策 | 部署 TTFB/运维 | — | 低 |

**建议路线**：~~A+B 先做~~ **已完成（全量口径 256.3ms = 0.682×，10/10）**。~~主线 = 引擎接入~~ **三步已落地（①库化 e7e4cd1 ②PyO3 787730a ③engine.py inproc 分支）**。~~python 宿主 GE 障碍~~ **已终结（2026-09-24 2dbffe9，GIL 劫持定罪 + allow_threads 修复）**。~~① C1+C2 连打~~ **已完成（2026-09-24 晚：C1 修复 0.654× + C2 probe 全过 2.41×，见 summary/2026-09-24_c1-vision-glue-fix-c2-int8-probe.md）**。**下一轮：① C2 int8 全链立项（第一动作 = 真权重误差谱 probe；预期全链 0.44-0.55×，行为风险 = flow 10 步欧拉的量化放大，golden/LIBERO 判）→ ② B 动态 L 定形（扩预置桶 + L 谱系探测脚本；验收 = 多任务集泛化——libero_spatial 等，兼作引擎推广性证据）→ ③ 顺手：spool 口径疑点全量回归顺判（task0 264 vs 直调 246 差 18ms）+ prefix asm token 查表缓存 + M2 eager 补 gate（技术债）**。A3 eval 全链 inproc 保持可选（abi 墙 + 宿主重写 vs ~5ms/帧收益，见 ④C）。

## ③ Export 标题建议

D:\compass\APXinf\syx_docs\dev_logs\chat_exports\2026-09-24_c1-c2-perf-wave2.md（性能第二波：vision glue 定罪+修复 0.654× + QuantMatmulDequant int8 单算 2.41× 全过）
