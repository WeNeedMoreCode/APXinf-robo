# 昇腾 NPU 移植 roadmap（跨 session 计划）

> 路线决策见 [decisions/001](../decisions/001-hybrid-route-with-native-seam.md)：混合路线 + 预留原生接口，硬件锁定 310P3。

## 阶段 0：环境与基线（✅ 2026-09-16 完成）

- [x] 连接远程服务器（root@192.168.13.119，8×310P3，共享机）
- [x] 摸清容器/镜像资源（MindIE 3.0.0、CANN 8.3/9.0.1、vllm-ascend）
- [x] 读取 ModelZoo pi05_openpi 适配文档（torch_npu + TorchAir，FP16，378ms）
- [x] 选定路线：pi05_lerobot（权重在 ModelScope 国内直连，免去 openpi jax→torch 转换）；容器 `apxinf_npu`（复用 MindIE torch2.9 镜像，torch_npu 2.9.0.post1 + CANN 8.5.1）
- [x] π0.5 checkpoint 到位：`/data/apxinf/weights/pi05_libero_finetuned`（7.0G，LeRobot 格式）+ `paligemma-3b-pt-224`（11G）
- [x] ModelZoo NPU 补丁分析归档（references/modelzoo-pi05-npu-adaptation.md）
- [x] **基线复现：合成帧 smoke 测试 P50 = 375.8ms**（ModelZoo 官报 375ms，chip 5，FP16，chunk 50×7；加载 175s + TorchAir 首编 123s 均一次性）。日志 `/data/apxinf/smoke.log`，脚本 `smoke_pi05_npu.py`
- 过程中解决的坑（详见 setup.md「依赖安装坑」+「容器内 transformers 补丁」）：torch 钉版连锁降级、gh-proxy git+ 前缀、numpy 2.x 破坏 pandas、batch 维缺失、CHW 布局、gemma warning_once 破坏图编译

## 阶段 1：torch_npu 后端接入 apxinf_robo API（✅ 2026-09-16 全部完成）

- [x] 读透 `src/`（apxinf_robo Python 层）：`engine.py` 是全仓唯一引擎依赖点，`Policy` 是结构化 Protocol（鸭子类型）
- [x] 引擎缝实现：`load_policy(engine="npu-torch")` 单点路由（engine.py +14 行），`NpuTorchPi05Policy` 新模块（wire HWC→CHW 转换、空相机零填充、eval+gradient_checkpointing_disable、模型侧 feature 名自动发现）
- [x] **端到端验证**：`build_robot_policy("franka_libero", ckpt, engine="npu-torch")` → `infer(obs)` → `actions (50,7) float32`；稳态 model_ms 378 / total_ms 395（chip 5）。日志 `/data/apxinf/e2e.log`
- [x] **bench 接入**：`scripts/bench_pi05_npu.py`（独立脚本，不硬塞 Rust 引擎版；L2 语义 warmup/samples/P50/JSON）。实测 model_ms P50 376.4 / total_ms P50 383.8，P90 仅 +2ms。产物 `/data/apxinf/bench_npu.json`
- [x] **CLI `--engine` 暴露**：`serve`（npu-torch 分支跳过 Rust 引擎 preflight，fp16-only 校验，拒 random-weights）与 `eval-libero`（选项过滤）都支持 `--engine npu-torch`，`--precision` 增 fp16
- [x] **L3 websocket**：`apxinf-robo serve --engine npu-torch` 起服务（8199 端口），最小 msgpack 客户端验证 metadata 下发 + actions 回传；稳态 infer_ms 384 / policy_ms 409。坑：首请求触发编译期间服务端无法应答 ws ping → 客户端 `ping_interval=None`
- [x] **noise= 精确注入**：临时替换 `model.sample_noise`（零填充到模型内部 32 维 padded 动作空间），finally 恢复。验证：同 noise 两次 max diff = 2^-10（1 个 fp16 ulp，aclnn 算子调度非确定性——**不保证 bitwise**），异 noise 分岔 0.25；horizon 形状护栏生效
- [x] **LIBERO smoke**：`eval-libero --backend in-process --engine npu-torch` 全链路：EGL 离屏渲染 + 520 步回放 + 104 replan + summary JSON 落盘。**success=False（0/1，单次无统计意义）**；**model_ms 1378（bench 的 3.7 倍，异常）**
- [x] **状态约定适配**：LIBERO wire 7 维（pos+axisangle+grip，openpi 约定）↔ LeRobot checkpoint 8 维（pos+quat+grip）——包装层 `_axis_angle_to_quat` 展开
- [x] **transformers 兼容改猴子补丁**（本仓实现，不动容器文件）：`_SilentLogger` 替换 gemma/siglip 模块 logger，dynamo 可追踪；已还原容器文件并实测验证
- 容器供给清单（pip/apt/libero 配置/资产）见 setup.md「容器内追加安装清单」

### 成功率问题：全链结案（2026-09-17 同日）

演进：**0/10 →（empty_camera mask 修复）→ 4/10 →（跨集 action queue reset 修复）→ 9/10 = 官方 `lerobot_eval` 同日同协议实测 9/10**（我们失败 task 7、官方失败 task 5——失败集不同证明残余 1/10 为 flow 采样随机性，非实现差异；口径已对齐：同 init[0]、同判定源）。两个根因与修复细节见 summary/2026-09-17_empty-camera-mask-root-cause.md（含追记）。

### 遗留待查 → 已全部结案

1. ~~**LIBERO 单步 1378ms vs bench 376ms**~~ → **均值陷阱**（2026-09-16）：无 warmup，首次 TorchAir 编译 100s 摊进平均；修复 `warmup()`，per_call 374.9ms ✓
2. ~~LIBERO success 0/N~~ → **根因：图像 uint8 [0,255] 直喂 LeRobot 链（期望 [0,1] float）→ siglip 归一化后 [-1,509] OOD**（2026-09-17 凌晨，summary/2026-09-17_libero-object-success-hunt.md）。修复 `_to_frame` /255 后 **libero_object task 0 成功（217 步，1/3）**——阶段一验收达成
3. websocket 服务端单线程阻塞——warmup 后基本消解

### 新遗留（2026-09-17 统计更新后 → 同日根因结案）

**核心未结 → 已结案**：libero_object 0/10 vs 官方 9/10 的根因是 **empty_camera_0 零图填充**——`_to_frame` 给占位相机补了零图，使 `PI05Policy._preprocess_images` 走 present 路径（mask=1，黑图 siglip embedding 参与 cross-attention）而非官方 missing 路径（全 -1 占位 + mask=0，相机被剔除）。零图×2-1 数值上恰为全 -1，与官方占位图逐像素相同——**分岔只在 mask 位**，故此前所有逐字段对拍全绿。修复：`_to_frame` 不补 `_empty_features` 键。

**定位方法（零 NPU 最小化对拍，`translate_parity_zero_npu.py`）**：单帧评测帧（reset+settle 后 t=0）双路径构造 batch——官方链（`LiberoProcessorStep` + preprocess，mini_official.py 语义）vs 我们链（`libero_images`/`libero_state_lerobot`/`_to_frame` + preprocess），frame/batch 两层逐字段 diff。单次 ~40s、全程 CPU 无模型加载，取代整集 NPU 跑（3min 加载 + 编译 + 1min/集）。图像 0.0 差（翻转/归一化全对）、state 3e-8、tokens 0.0、task 字符串全同、**唯一分岔 = empty_camera_0 键存在性**。

**顺带排除的嫌疑**：图像 180° 翻转（两侧都有：官方 `LiberoProcessorStep` flip dims=[2,3]，我们 `libero_images` [::-1,::-1]）；env 初始化（两侧同为 reset → set_init_state → 10 步 settle[-1]、seed=7）；quat→axisangle（公式同、float64 vs float32 差 7e-9）；调度语义（两侧同为 `policy.select_action` 同一函数）。

**官方协议复测**（10 任务 × init[0]，replan=0，chip5，`/data/apxinf/mask_fix_eval.log` + `summary_mask_fix.json`）：**4/10**（task 0=143 步/1=123/5=175/8=276 成功），修复前 0/10——mask 根因坐实。**残余差距 4/10 vs 官方 9/10 待查**，下一步候选：核对官方 9/10 当次的确切条件（init/seed/成功判定——官方为视频帧数判定 vs 我们 env.check_success 接触判定；判定口径本身可能贡献差距）、flow steps/超参、以及失败集（2/3/4/6/7/9，全部 520 步打满）的行为 trace。

**其他待办 → 已收尾（2026-09-17 晚，阶段 1 全部完成）**：

- **replan 1/5/50 重测**（10 任务 × init[0]，chips 5/6/7 并行）：replan=0 → **9/10**（挂 task 7）；replan=1 → **8/10**（挂 task 4、9；每步重采样噪声暴露最大）；replan=5 → **9/10**（挂 task 6）；replan=50 → **10/10**。四种配置失败任务各不相同且 ≤2——与"残余失败为采样随机性"结论一致；旧注释"该 checkpoint 对 noise 重采样敏感"系污染数据误报，已从代码删除
- **bench 复测**：model_ms P50 376.0 / P90 378.3（修复前 376.4/378+）——不变，确认两处修复不动热路径；warmup 104.9s 单列未污染样本（`bench_npu_refix.json`）
- **websocket ping 生产化**：`serve --engine npu-torch` 加载后、监听前调 `warmup()`（编译前置）；实测日志顺序 `warming up` → 98s → `server listening`，客户端首请求 infer_ms **381**（稳态，非 100s+ 编译），loop 最长阻塞 ~0.4s << ping 超时，客户端无需再设 `ping_interval=None`

## 阶段 2：Rust 原生 apxinf-ascend（逐个过相异点）

- [x] **工具链 + FFI 地基验证（2026-09-17 晚，M2 第一腿）**：容器 `apt install cargo`（1.75.0，够用）；手写 ACL FFI 冒烟 crate `/data/apxinf/ascend_smoke/`（本地镜像 `syx_docs/dev_logs/ascend_smoke/`，**无 bindgen/clang 依赖**——签名直接抄 acl_rt.h）通过：`cargo build` 链接 `libascendcl.so`（build.rs 里 link-search=/usr/local/Ascend/ascend-toolkit/latest/lib64）+ aclInit→SetDevice→Malloc→H2D/D2H 往返（4096 字节 0 错）→Free→Finalize 全 ret=0。运行需 `LD_LIBRARY_PATH` 带 CANN lib64 + `ASCEND_RT_VISIBLE_DEVICES` 挑芯。分派缝形态已摸清（`apxinf-model/src/accelerator.rs`：`Device::Cuda(id)` + `#[cfg(feature)] mod`，ascend 同构接入）
- [x] **ACL FFI 骨架（2026-09-17，commit 1c76783 @ fork/ascend-port）**：`apxinf-ascend` crate 正式化——手写 aclrt FFI（acl_base/acl_rt，无 bindgen）+ `AscendContext` RAII（OnceLock 幂等 aclInit + SetDevice/CreateContext/Drop 清理）+ `DeviceBuffer` RAII + h2d/d2h/synchronize。rlib 不最终链接 libascendcl（非 Ascend 机器可编译），example 真芯冒烟通过
- [x] **`Device::Ascend(usize)` 枚举 + 分派缝（同 commit）**：core 枚举/is_gpu/Display，model 分派缝 + auto + llama 全部穷举点带 "not wired yet" 臂，py 的 parse_device("ascend[:N]")。cargo feature `ascend` 的元包链待第一个 Backend 算子时挂
- [x] 子模块分支策略已定：fork `WeNeedMoreCode/ApxInf` 的 `ascend-port` 分支（外层 .gitmodules 已指向 fork），引擎改动两步走（子模块 push + 外层 bump gitlink，CLAUDE.md 有纪律）
- [x] 工具链（服务器）：Rust 1.85.0 @ /data/apxinf/rust（本地代理下载+传输；apt 1.75 是死路：lock v4 + MSRV）；crates.io 走 rsproxy sparse。详见 setup.md「Rust 工具链」
- [x] **CANN 9.0.1 工作容器 `apxinf_rust`（2026-09-17）**：ACLGraph 激活环境（8.5.1 拒 207000），privileged + driver 挂载 + /data 共享（Rust/引擎副本零迁移）；共享 CARGO_HOME=/data/apxinf/cargo + `source /data/apxinf/rust_env.sh`
- [x] **graph.rs：ACLGraph capture/replay（53ec764）**：aclmdlRI* FFI + `AscendStream`/`AscendGraph` RAII + abort；graph_smoke 三模式（GLOBAL/THREAD_LOCAL/RELAXED）真芯验证 replay 回写 pattern
- [x] **tensor.rs + 第一个计算算子 aclnnMatmul（5804a92）**：`AclTensor` ND 描述符 RAII（fp16/fp32）；matmul_fp16 两段式 + workspace；对拍 CPU fp32 参考误差 4.8e-4。链接面：aclnn 在 **libopapi.so**（非 libascendcl）+ 传递依赖 libnnopbase.so
- [x] **元素/融合算子组（450f801）**：add（aclScalar alpha RAII）、silu、add_rms_norm（融合三输出，rstd 隐藏分配）+ `two_stage` 共享 runner；误差 2e-4~1e-3。踩坑：rstdOut 必须 2-D [rows,1]，1-D 报误导性 561103（NULLPTR 名不副实）
- [x] **PFA attention（a925b9a）**：`aclnnPromptFlashAttentionV3`（老版 2026-12 弃用，直接绑 V3）；全量无 mask BNSD，quant 槽位 null；对拍 CPU softmax-attention 1.4e-3
- [x] **AscendBackend 实现 Backend trait（9d82b6c）**：matmul/add/mul/scale/silu/rms_norm（融合 add_rms_norm + 零 buffer 复用）/synchronize/**begin/end_capture（ACLGraph 走通 Box\<dyn Graph\>）**/to_device/to_cpu（f32↔f16，ModelZoo 语义）。Storage 复用 Gpu 槽位（Arc\<DeviceBuffer\> 塞 _prevent_leak，downcast_ref 借回）。rope/embedding/kv-cache/sampling 为显式 "queued" 错误臂。backend_smoke：matmul 0.0 / silu 7.9e-4 / rms_norm 9.3e-4 / capture-replay ✓
- [x] **feature 挂接 + M2 构建半（06f2b60）**：`ascend` feature 贯通元包/apxinf-py（cuda 同构）；9.0.1 容器验证 `cargo check --features ascend`、`check -p apxinf-py --features ascend`、**`build --release -p apxinf-py --features ascend`（cdylib）** 全过
- [ ] **下一章：PI0.5 executor（M2 运行半 + M3 的主体）——结构侦察完成（含一次认知修正，2026-09-17）**：
  - **⚠ 认知修正**：初判"bf16_runtime 设备无关可零改动复用"**错误**——它深绑 cuda（`use super::backend::{kernels, transfers, CudaBuffer, RuntimeBackend}`，计算直调 `gemm::bf16(ctx,..)` / `elementwise::*` 等 cuda kernel 门面，中间量全是 CudaBuffer）。正确结构：vla_runtime → bf16_runtime → pi05/backend.rs(kernels 门面) → apxinf-cuda
  - **结论：executor = 镜像移植**（对照写 ascend 版：kernel 调用层用我们的 ops 替换 gemm/elementwise 调用 + runtime 层把 CudaBuffer/ctx 换 AscendBackend/DeviceBuffer，约 1000+ 行新代码）。**全部为新增文件，不动 cuda 活代码 → 无 CUDA 回归需求**（用户有 CUDA 机器兜底，动 cuda 代码时写最小验证脚本由用户手动跑）
  - **真正可复用（已核实）**：`bf16_weights.rs`（权重上传走 `backend.to_device` trait，BF16→F16 免费转换）+ `config.rs` + `static_weights.rs` 系（host 侧权重解析）+ `fp8.rs` 的量化数学（host）
  - `create_normal_generator` **已完成（4ea8249）**：flow noise 用 core Philox + h2d；rope/embedding/sdpa/kv_cache 不在 pi05 路径（后置）
  - **镜像施工图（2026-09-17 终版，零探索成本可开工）**——依赖链 `vla_runtime(1013) → bf16_runtime(690) → bf16_executor(228) → kernels`，全部新增文件不动 cuda：
    - **待补 op（映射表）**：~~gather/gelu~~ **已完成（37140a4）**：gather → aclnnGatherV2（embedding 通路 bit-exact）；gelu → **aclnnGeluV2**（正向定位战果：aclnnGelu/FastGelu 在 310P3 算子包空缺——core dump / 161002 支持列表为空，跨 8.5.1/9.0.1；torch_npu 真实分发即 V2，tanh/erf 双模式真机验证，误差 4.8e-4）。**连带修复 cat 的 TensorList double-destroy**（aclDestroyTensorList 连带释放子描述符，RAII 二次销毁污染注册表致后续算子 plan segfault——二分定位）。两坑已入 skill runtime_pitfalls 第 13 章。`attention::mha/mqa` → `prompt_flash_attention_fp16`（KV buffer：预分配 + offset 视图）；~~`rope`~~ **已完成（96521c2）**：融合入口双双拒绝 π0.5 的 head_dim（ApplyRotaryPosEmb ∈{64,128}、RotaryPositionEmbedding ∈{32,64,96,128}，π0.5 用 256/72）→ **组合版主路径**（gather 通道重排 bit-exact + mul×2 + add，sign 折进 host sin 表——mul 拒绝零步长广播而 add 接受），真实 D=256 验证 3.9e-3；V2 变体留作单算子优化项；`norm::layer` → aclnnAddLayerNorm(x, zeros) 或组合；`fused::*` → add_rms_norm/bias_add/add 组合；`split_qkv` → AclTensor offset 视图。**ops 待办队列已清零——下一步直接进 B（ascend_executor 层函数镜像）**。**B 的全部建材亦就绪（44b1a2a）**：layer_norm（fused AddLayerNorm+zeros，2e-3）、row_replicate（gather 广播替代，bit-exact）、take_rows（D2D 行切分，QKV split 用；aclTensor offset 视图语义不可靠已移除并留禁用注记）。ops_smoke 12/12 全绿。层函数镜像可直接复用 bf16_weights 的 Tensor 容器（dtype F16 由 to_device 转换）
    - **施工顺序**：A. ~~补 ops~~ ✓ → B. ~~executor 层函数镜像~~ ✓（含真权重路径冒烟：权重上传/输入上设备/层内前 3 个算子全部真机跑通）→ C. **卡点：大 matmul（详见下）** → D/E/F 待续
    - **⚠ C 阶段核心卡点——大矩阵 matmul 在 310P3 的可行路径（2026-09-18 排查现场）**：
      - **已证伪两条路**：① `aclnnMatmul` ND 路径大矩阵崩——kernel `MatMulV2_NZ_ND_FP16` 对 [8,2048]×[2048,2560] 触发 aicore 异常 `MTE DDR address out of range`（小矩阵如 64×128×96 正常；日志铁证）；② `aclnnMatmulWeightNz` **310P 不支持**（日志原文 `Weight NZ is unsupported by the current SOC version [Ascend310P]`——A2/910 系专属）
      - **已废一条路**：`aclnnNpuFormatCast`（ND→NZ 设备转换）desc 要求无法从公开 `aclCreateTensor` 满足（ori_shape 语义，参数看似全对仍 161002）；已改 **host 侧 NZ 重排**（`host_nz_reorder`：CPU 16×16 分块 W-major + 上传，语义自控，代码已在库）
      - **2026-09-18 深夜第二波实验（新证据，改写排查图景）**：`matmul_layout_probe`（独立进程、生产 shape [8,2048]×[2048,2560]）：① plain b row-major **隔离跑不崩且数值对**（4.9e-4）——推翻"大 shape 必崩"；② 前置一个 AddRmsNorm 后：plain b **数值错 0.79**（不崩但读错数据）→ **AddRmsNorm 污染后续 ND-matmul 状态**（cat TensorList 同款类）；③ **转置 b（[N,K] 物理 + [1,K] 转置 stride 视图，torch w.t() 布局）在前置 rms 后仍数值正确**——aq::matmul 已切此路径（NzCache 改 host 转置缓存 + `matmul_b_t_fp16`）；④ **ascend_layer_smoke 全序列仍 507015 崩**，kernel 变为 `MatMulV2_NZ_ND_FP16_false_true`（转置变体）——**存在第二层差异**
      - **第三波（深夜收口）**：smoke 崩点精确定位在**第 25 个 op（down proj matmul，K=8192）**——前 24 个 op（含 2 次 K=2048 matmul、AddRmsNorm、GeluV2、Mul）全过，转置路径已生效；**K 阶梯 probe（4096/8192/16384 转置）隔离全 OK**——K 非独立因素，**纯序列污染**（probe 复刻 smoke 序列才能逼出污染步）
      - **第四波（2026-09-18 收口）**：PFA(BSH, smoke 真实头几何) → down-matmul **OK**——PFA 无罪。**剩余嫌疑 4 个（全是数据搬运类）**：① take_rows 大切片（32MB D2D）② kv_bias 的 d2h→h2d 往返 ③ NzCache.get 的大权重 d2h→transpose→h2d ④ rope 的 pos-gather 组合
      - **✅ 第五波结案（2026-09-18 深夜，卡点清除）——三个根因，两个是 probe 自身的坑**：
        1. **读回竞态（最大坑，推翻多个"静默读错数据"结论）**：probe 的 `copy_d2h().and_then(synchronize())` 顺序是竞态——同步 `aclrtMemcpy` **不等待自定义 stream 上的异步 kernel**，先读后同步 = 读到 kernel 写完前的旧数据。修正为先 sync 再 d2h 后：N=8192 全部 M（8~256）plain/t-b **全部正确**；此前"t-b 在 N≥4096 静默错"是竞态伪影。**生产代码同步修复**：`host_f16_row`（读 style 张量，matmul 异步产物）补 stream sync；NzCache/kv_bias 读权重（同步 h2d 写入）安全无碍
        2. **唯一真实硬件缺陷 = 超小 M × 超宽 N 的 aicore tiling fault**：m=8 × N≥12288 两种布局都真崩（sync 507015，真 aicore fault 非 race）；m≥16 × N=16384 全对。smoke 的 tokens=8 恰好踩中 gate_up（m=8, N=16384）——此前"op#25 down matmul"系计数误标，真正崩的一直是 gate_up。**修复：M 填充**——`MATMUL_MIN_M=16`，m<16 时 memset 零 + 单次 D2D 拷贝（行追加=前缀连续）→ matmul → take_rows 切回，实现在 `matmul_fp16`/`matmul_b_t_fp16` 内部，所有调用方免费获得。m 阶梯复测 m=8×N=16384 两布局全对
        3. **plain-ND-b 描述符的前序状态敏感（独立真实现象，未修不阻塞）**：AddRmsNorm 之后再跑 plain [k,n] 连续 b 的 matmul（N=2560, m=8，**有 M 填充**）仍读错 0.79；转置视图（stride [1,k]）免疫且正确。机制推断：ND 连续 b 走的 MTE 搬运路径对前序内存/缓存状态敏感，转置 stride 强制选到正确 kernel 变体（`MatMulV2_NZ_ND_FP16_false_true` 的 transB 旗标）。**生产全走转置路径（NzCache/aq::matmul），plain 路径降级为仅 probe 用**
        - 佐证：torch_npu 同容器同 shape（mm internal format=ND）两种布局 4.9e-4 全对 → API 层面无问题，坑在我们直连 aclnnMatmul 的描述符路径；8.5.1 容器跑同一 probe 二进制同崩 → 非 9.0.1 回归
      - **验收：`ASCEND_LAYER_SMOKE_OK`（2026-09-18）**——真权重路径全 language 层前向通过（[8,2048] 输出全有限，首调 2.05s 含 warmup）；layout_probe variant 5 全序列复刻 seg0~seg10 端到端通过。新增 aclnnMm/aclnnGemm FFI 与 ops 入口（bake-off 用，未进生产路径）
      - **✅ C 阶段完整收官（2026-09-18 深夜）：`ASCEND_FULL_SMOKE_OK`——全模型随机权重前向跑通**（vision tower 2 层 + prefix language 2 层 + 10 步 flow denoise，3 视图 768 patch + 60 token，输出 [50,32] 全有限，首调 4.94s 含全部 warmup/转置缓存）。新增 `ascend_runtime.rs`（`Pi05AscendRuntime` 镜像 bf16_runtime 的 eager 路径：encode_vision/embed_prefix/prefix_forward/styles/denoise/infer；ACLGraph 捕获后置——ada-norm 的 host 读需先预解析）。**过程中取证定位并修复 6 个真 bug**（trace 标记 + NzCache 形状打印是决定性证据）：
        1. **action 层 inter 双倍**（根因）：inter 从融合 gate_up 宽度 [width, 2*inter] 算出后再乘 2 → b 描述符 2×越界；NzCache 转置形状 [512,16384] vs 正确 [1024,8192] 一眼定罪。越界读是否踩未映射页取决于分配邻接 → 伪装成"上下文依赖/累积污染"（tokens=828 过 layer0 崩 layer1、一切隔离复刻全过）
        2. **vision 语义对齐**：position 表按 view 循环 gather（原 cat 两次硬编码 2 视图，π0.5 是 3 视图）；SigLIP attention 按 view 分段 batched BSH PFA（原 768 全量）
        3. **gate/up 顺序**：from_host_parts 拼接是 [gate; up]，gelu 应加前半（原反了，输出有限所以没炸）
        4. **cross-length 前缀 attention**：action PFA 的 q/kv 序列长度不同（q=块、kv=前缀+块），原入口共用一个 tokens → q 描述符越界读；新增 cross 变体
        5. **zeros 按字节分配**：两处 vision layer-norm 传了元素数（buffer 减半）
        6. bias_add None 分支恒等拷贝参数非法；M 对齐规则泛化（宽 N × 非 16 倍 M 崩：812/820/828×N≥16384 全崩、832 全过 → 无条件 pad 到 16 倍数）
      - **C 剩余**（matmul 已通）：~~ascend_runtime.rs 镜像~~ ✓（eager 全通）→ D vla 镜像 → E 注册 → F bench = M2 运行半
      - **✅ D+F 完成（2026-09-18 深夜）：`ASCEND_RANDOM_BENCH p50=197.5ms`（n=20，depth 2/2/2 真实宽度）= M2 运行半达成**。新增 `ascend_vla.rs`（`Pi05AscendVlaRuntime`：VlaRuntime trait 的 eager 实现——patches 输入、core RNG 噪声、时间嵌入一次性构建；rgb-u8 patchify/tuning/capture 桩化）+ `ascend_random_bench` example（走 prepare/run 真实接口）。**连跑无 sync 的 bench 揭出两个异步生命期地雷并修复**：
        1. **scratch 复用池**（`AscendContext::scratch_buf`，按大小缓存永不释放）：rope pos/cos/sin、ada-norm style 行、M-pad 暂存、kv-bias 切片等短命 buffer 曾在喂给异步 op 后立即 drop——aclrtFree **不按 stream 排序**；单 stream 顺序执行保证同尺寸复用安全
        2. **op 输出延迟释放**：`DeviceBuffer::drop` 改为 park 到全局 pending 列表，`AscendStream::synchronize` 排空队列后统一 free——中间量（q/k/attn/proj/…）曾边排队边被 free 再被下一 malloc 复用（sdma copy error 0x217）。⚠ ACLGraph 接入时需处理 capture 窗口内的 flush 语义
        3. **ada-norm 语义对齐 cuda kernel**（normalization.cuh 铁证）：`y = rms(x)·(1+style[0:w]) + style[w:2w]`——style 投影是 [w, 3w] 且带 **shift 加法段**（此前镜像漏掉）
        4. q/k/v/mlp bias 可选化（synthetic 权重无 bias）
      - **E 剩余**：mod.rs/auto 注册链（accelerator 分派 Device::Ascend → load → Pi05AscendVlaRuntime）+ py 绑定暴露；之后 = 全深度 random bench + 真 checkpoint 加载（M3 前置）
      - **✅ E 完成（2026-09-18 收官）：注册链 + 真 checkpoint 全链路跑通**
        - `pi05-ascend` 注册（builtin.rs → `pi05::register_builtin_ascend` → `ascend_vla::load_registered`：Arc\<dyn Backend\> 经 as_any downcast 还原 Arc\<AscendBackend\>（镜像 cuda seam），synthetic/safetensors 共用设备无关 host 权重管线）。`ASCEND_REGISTRY_SMOKE_OK`——AutoModel → LoadedModel::Vla → trait 对象推理，**全深度（27/18/18）随机权重**通过
        - **`ASCEND_CHECKPOINT_SMOKE_OK`——真 checkpoint**（LeRobot 7GB safetensors，`/data/apxinf/weights/pi05_libero_finetuned`）：加载 125s（bf16 解析+f16 上传）、首调 63.2s（惰性转置缓存）、**稳态 1.93s**、1600/1600 有限。placeholder token ids（真 tokenizer 随 LIBERO 集成落地）
        - **阶段 2 主干全部贯通 = M2 完整（构建半+运行半），M3 起跑线就位**
      - **优化待办（性能阶段，目标对标 378ms torch_npu 基线，当前 ~1.9-2.0s）**。**瓶颈实测排除法（2026-09-18 收官）**：ada-norm style 缓存（已做）收益≈0（d2h/sync 仅 ~27ms/1.4%）；workspace 复用（已做，`two_stage` 走 scratch 池，顺带消除 workspace UAF）~2% → **剩余大头 = plan 阶段 host 开销（每 op GetWorkspaceSize/tiling ~400µs × ~4600 op）**。**ACLGraph 捕获前置已全部落地（c6b24f4）**：style 行缓存 ✓、kv_bias 预切缓存 ✓、rope pos 索引 pin ✓、层内路径 sync 清零 ✓——denoise 路径现为纯设备流。**剩余唯一 capture 阻塞 = 中间 buffer 生命期**：op 输出在队列中途 drop（延迟释放救正确性，但 graph 烘焙的地址会在 sync 后被 free 复用）→ 需要镜像 cuda `GraphWorkspace` 的 arena（捕获模式 malloc 走 bump 分配、随 graph 存活）。优先级：① ✅ **ACLGraph 捕获已跑通（2026-09-18 深夜，28857f0）：`ASCEND_GRAPH_CAPTURE_SMOKE_OK`——warmup→styles 预计算→arena（1.76GB）→RELAXED 捕获→replay，对拍 0.0068，depth 2/2/2 下 replay p50=88.1ms vs eager 305.1ms = 3.46×**。关键根因：GLOBAL/THREAD_LOCAL 模式拒绝 aclnn 执行器内部的同步 memcpy（107030，官方文档确认）→ RELAXED 解除；窗口内残留的两个 stream sync（denoise mark closure + trace mark）清除——mark 全部 print-only（sync 会扰动捕获态，107027）；euler 的 aclnnMuls 标量路径换张量常量乘（euler_consts 缓存，σ 步常数）。② **全深度捕获：子图上限 ~2000（建议 1800，vllm-ascend 文档）→ 全深度 ~4600 op 需拆图策略**（按 transformer 层或 flow step 分段捕获多图接力；参照 vllm-ascend ACL Graph 设计文档的多图接力模式）③ **热点融合（与 378ms 竞争的真正战场）**：rope 换单算子（V2 变体查 head_dim 支持或 AscendC 自研——项目 skill AscendC-ops-dev 已备）、ada-norm 四算子组合收缩、matmul tiling 布局 ④ checkpoint 全深度 capture 版 smoke + bench
      - **⭐ 性能验收线（与用户约定，2026-09-18）**：拆图捕获 + rope/ada-norm 热点融合完成后跑全深度 checkpoint bench——**≤378ms 或接近 → 引擎价值成立（部署形态 + 延迟打平），进 M3（LIBERO 对标）**；**仍差 30%+ → 决策会谈**（选项：接受"略慢换部署形态"／310P3 定位为验证芯片、性能目标移至下一代硬件）。诚实外推当前 ~550-850ms（88.1ms/2/2 × 全深度系数，拆图前不准）。差距本质：捕获消 host 开销后，剩余是设备侧组合实现（算子数 2-4×）vs TorchAir 融合 kernel——靠逐热点融合追
      - **✅ 性能阶段推进（2026-09-19 凌晨）：全深度 906.9 → 680.8ms**。三项落地：
        1. **拆图前提证伪（官方文档取证）**：~2000 上限是**图实例个数预算**（每图耗 1 stream，MindSpore 文档原文），非单图节点上限；vllm-ascend 分段的原因是 torch.compile 路径 attention 需要 host 侧参数更新，与我们无关。**全深度 27/18/18 单图捕获直接成功**（~4600 op，arena 8.3GB，对拍 0.0155），无需拆图。多图接力也已验证可行（三段捕获链式 replay 对拍一致，接力开销 ~3ms）——ascend_graph_capture_smoke 的 APXINF_SEGMENT_BENCH=1 分支即拆图原型
        2. **scratch 池语义修复**：原 pop-借走无归还 → 池从未命中（捕获窗口内同尺寸 workspace 每 op 一份进 arena）。改共享不回收（同尺寸一份永生，单流顺序安全）→ arena 1.76GB→665MB（mini），eager 也 305→187ms
        3. **rope 平铺重写 + pos 表缓存（-228ms）**：msprof 分桶定罪——rotate-half 的 axis=1 列 gather 在 language 层单次 6.5ms（带宽下限的 49×）。重写为 `[rows,d]→[rows*2,d/2]` 平铺视图上的邻行交换 gather（axis=0 连续行，cos 半维重复/sin 符号折叠使表可同视图复用，数学对拍 0.007~0.025 噪声级）+ cos/sin gathered 表与 swap 索引按 (tokens,heads,offset) 缓存（纯常量，原先每层重 gather 0.4-6.5ms）。GatherV2 族 126ms→3.9ms
        - **双边 profiling 基建**（同一把尺子）：torch_npu 用 torch_npu.profiler + msprof sqlite（ai_core_op_summary.db 的 ge_summary⋈task_time，duration 单位 ns）；Rust 侧用 msprof --application 挂 wrapper 脚本。torch_npu 397ms 构成：MatMul 124ms / **TransData+Transpose 100ms（NZ 权重代价，我们架构性避开）** / Softmax+BatchMatMul 36ms（attention 拆开跑，我们的 PFA 29ms 反超）。我们 680.8ms 构成：MatMul 213ms（prefix 大 matmul 已 19-21.6 TFLOPS≈峰值，**无需动**）+ Add 78ms（ada-norm shift-add 主力）+ PFA 29 + GeluV2 29 + Mul 14 + **kernel 间隙 ~289ms（~1900 kernel × ~150µs）**
        - **下一波（数据驱动，预计 → ~500ms）**：① ~~ada-norm mul+add → `aclnnAddcmul`~~ **已证伪（2026-09-19）**：torch_npu 可用（对拍 0.005）但实测无时间收益（684.4 vs 680.8ms，3 输入 kernel 不比 mul+add 快）且 fp16 累加顺序漂移对拍到 0.048（贴 0.05 断言线）→ 撤回，op 保留带结论注释 ② ~~gelu+mul → `aclnnGeGlu`~~ **310P3 挂死（同日）**：两段式调用永不返回（10 分钟 0 退出，kill 取证）；torch_npu 无分发入口、GE 图从未融合它——三重证据判不可用，撤回组合保留 ③ **AscendC fused ada-norm（5→1）已建成（98aabfc，2026-09-19）**：完整工具链（cmake/编译/dlopen/对拍/捕获）全绿——**replay 性能打平（679.8 vs 681.6ms）**。三次实验（广播缓存 -2ms / addcmul +4ms / fused -2ms）一致收敛到同一结论：**图内设备时间由带宽决定，kernel 数不是成本**——3 个 aclnn kernel 的总访存 ≈ 1 个 fused kernel 的访存（带宽下限）。fused 的真实价值 = eager 路径提速（launch 开销消除，eager 1924→2030ms 波动）+ 为后续自研 kernel 铺平工具链。取证战果：**硬件 ReduceSum/RmsNorm 高级 API 被 `__NPU_ARCH__ ∈{2201,3510,5102}` 门控——310P3 不在列**（torch_npu 无融合 rms 的根因），树归约 Add 是 310P3 唯一路径；**aclrtlaunch 可进 RELAXED 捕获窗口**（replay 对拍 0.019 全深度）；跨行软件流水线陷阱在 kernel 内复现并被 GetValue 双 pin 修复（skill TBuf 陷阱的 kernel 内同款）
        - **剩余唯一大项：~290ms 空隙的来源 → 已取证闭环（2026-09-19，a7cdde5）**：msprof timeline gap 分析（ge_summary⋈task_time 按 start_time 排序，只计链内 <5ms 空隙）——**链内空隙 ~401ms，81% 集中在 MatMulV2 task 之前（avg 334µs × 970 次/链）**；元素 op（Add/Mul）启动仅 3-6µs、Gather 65µs、Gelu 115µs。即 **ACLGraph 下 matmul task 启动税 ~334µs/次** 是此前一切"删 kernel 不省时"的根因（广播缓存 -2ms / addcmul +4ms / fused ada-norm -2ms 三次实验全部自洽）。**换 matmul 入口判死**：aclnnMm/aclnnGemm/MatMulCommon 图内背靠背 100/16 连发三档同分（m=832 均为 5.86ms = 20.2 TFLOPS 含启动税）。torch_npu 124ms matmul 的组织学根源：GE 静态 OM 编排（task 间无启动税）+ matmul task 数 ~105/iter（批量融合组织）vs 我们 970
        - **⭐⭐ 性能验收线判定（2026-09-19，插桩三轮闭环后提交用户决策）**：当前 679.8ms（全深度单图 replay，对拍 0.019）。**插桩结论（用户要求的定位 gap，三轮）**：① 同口径 msprof gap 分析——torch_npu（GE 静态 OM）13230 task/iter 链内空隙仅 **17.3ms**（TransData 前 3000 次 gap=0µs 背靠背）vs 我们 401ms；② **无 profiler 的 m-scaling 拟合**（m∈{64..832} 图内连发）：`time = 6.1µs/row + 475µs/task`——**每 matmul task ~475µs 固定启动税**（与 832 档 5.86ms 实测自洽），部分与前驱元素 kernel 重叠故全链墙钟空隙 288ms < 970×475µs；③ qkv 本就已融合（C 阶段产物），970 matmul task 是模型结构下限。**根因定性：ACLGraph（aclmdlRI 捕获模型）的 matmul task 调度税 vs GE 静态 OM 零税编排——架构级差异，aclnn 层无解**（换入口三档同分、op 融合三次实验不动）。用户决策：**C 路线（GE 原生 OM）POC 先行**——GE 是纯 C++ 原生库（无需 torch），API 面已核实（graph.h/operator_factory.h、aclgrphBuildModel、atc、内置算子 MatMulV2/LayerNormV4/RmsNorm/PromptFlashAttention/ApplyRotaryPosEmb/GeluV2 全在）
        - **✅ C 路线 POC 完成（2026-09-19，28adf78）——零调度税坐实，C 路线（GE 原生 OM）收益成立**。`ge_poc_main verify` 对拍 0.002075（fp16 双链 vs fp32 参考）+ bench：**m=832（prefix 主战场）GE OM 4.745 vs aclnn+ACLGraph 5.812 ms/matmul（−18%，23.5 TFLOPS）**；m-scaling 拟合 aclnn `5.86µs/row+555µs/task` vs GE `4.71µs/row`（固定税消失、斜率再降 20%——静态 shape 专属 tiling 优于 aclnn 运行时 tiling）；小 m（64-256）两者打平（±5%，kernel 效率主导）。外推全模型：~290ms 空隙消除 + prefix matmul 提速 → **~370-390ms ≈ 验收线 378ms 附近，C 路线值得全量投入**。**修复链（全部取证定位）**：① 纯 operator 流构图——删 AddNodeByOp（它与 SetInputs 是互斥路径：SetInputs 自己从 OperatorImpl 双侧链接物化整图，AddNodeByOp 先 init 空内图锁死 SetInputs 且节点不带边）；② 经典 MatMul 替代 MatMulV2（V2 的 MatMulV2InferShape 有 rank∈{2,4} 门槛，拓扑+desc+origin shape 全对仍拒——读取的不是我们设置的 desc，待查开放项）；③ build_options 传 `input_shape`+`input_format`（atc 语义，`Impl::SetInputs` 从选项 map 解析 Data shape——不传则编出 9KB 无 kernel 空模型、IO dims 全空）；④ **Data 幽灵 input desc 0**（模型 IO dtype 从 `GetInputDescPtr(0)` 读——不设则 fp32 IO + 插 Cast kernel → 全零输出；首个空模型 in size=4=fp32 标量即此症状）。**环境增量**：**9.0.1 rust 容器 GE 全通**——TeFusion py_decouple 起不来仅因缺 `python3-config`（strace 取证：`sh -c "python3-config --prefix"` 退出 127 → dynamic-handle 失败），`ln -s python3.12-config python3-config` 即修，npu 容器不再必需；CANN 9.0.1 与 8.5.1 双容器 OM 产物同症状同修。**C 路线全量施工计划（2026-09-19 定稿，三段式）**：
  - **C1 垫脚石**（① ge_poc shim 泛化为 `ge_builder` 通用构图 FFI——`add_data(name,index,dims,dt)/add_op(name,type)/set_input_desc/set_output_desc/set_attr_{bool,int64,str}/set_input(dst,port,src)/build(input_shape)/run(bufs,stream)/save(path)/load(path)`，模型定义留在 Rust、C++ 只做薄层；② 权重布局单点实验：MatMul `transpose_x2=true`+`[n,k]` desc 对齐 NzCache 转置管线（POC 用 `[k,n]` 无转置——全模型权重是转置布局，首个钉死变量），对拍+bench；③ 单层 language layer 全序 GE 化：qkv(已融合)→rope 组合(GatherV2/Mul/Add)→PFA→proj→rms/ada 组合→gate_up→GeluV2·Mul→down→residual，双路径对拍（GE OM vs eager aclnn 同输入，容差同现有 smoke ~0.02）；④ 单层 OM bench vs 同层 ACLGraph 捕获）
  - **✅ C1-①②③ 完成（2026-09-19，1691df9）——ge_builder FFI 全链绿 + 转置布局零代价 + PFA GE IR 定案**：
    - **① ge_builder**（`ascendc/ge_builder/` C++ 薄层 + `src/ge_builder.rs` Rust 客户端 + `ge_builder_probe` 例程）：多模型槽并存（C2 三段 OM 预留）、IO introspection（加载时缓存 size/dims——run 免 aclmdlGetDesc 往返）、save/load OM 往返、同步执行排障变体。C++ 驱动复现 POC verify **逐位一致（0.002075）**；Rust 全链（模型定义在 Rust）对拍 aclnn **0.00009**、bench **4.649 ms/matmul**（m=832 n=32768，POC 参考值 4.745）。**API 可查错性修正**（9.0.1 operator.h 实测）：`UpdateInputDesc/UpdateOutputDesc` 返回 graphStatus（已检查、可报错）；`SetInput/SetAttr` 链式不可查；`Graph::SetInputs/SetOutputs` 也是链式（IsValid 是唯一检查）——skill 回填时修正"全部不可查"的旧结论
    - **② 转置布局零代价**：MatMulV2 `transpose_x2=true` + [n,k] 物理 desc，数值对拍 0.002075 同分；bench **4.930 vs 4.941 ms/matmul 打平（同日同进程矩阵）**——NzCache 转置管线权重直接入图，无需回转。经典 MatMul + transpose_b=true 编译失败（TBE task_distribute 报错，plog 无细节）——不阻塞，V2 双布局全通（POC 期 V2 rank 门槛在 ge_builder 路径未复现）
    - **③ PFA GE IR 定案**（取证路径 = 310P opp 包 kernel json，零编译试探）：注册名 **`PromptFlashAttention`**，13 输入（query/key/value fp16 必选 + 10 可选：pse_shift fp16 / atten_mask bool / actual_seq_lengths(_kv) int64 / deq·quant 系），输出 `attention_out` fp16，attrs `num_heads/scale_value/pre_tokens/next_tokens/input_layout/num_key_value_heads/sparse_mode/inner_precise`——与 aclnn 侧参数一一对应；`optionalInputMode=gen_placeholder`（可选项自动占位）。**310P 有预编译 kernel**（命中静态 key 免编译秒过）。**附加战果**：同目录 `apply_rotary_pos_emb`（RoPE 单算子——性能阶段"rope 换单算子"待办的 GE 图内路径）与 `incre_flash_attention`（decode 侧）也在 310P 预编译列表
    - **探针数据教训**（已记入代码注释）：链式随机 matmul 对拍需权重 std ~0.01（每对增益 = std_w²·√(k·n)）；/400 权重第 6 对起 fp16 饱和，双路径在 ±65504 异号——假性分歧 131008（旧 ge_poc_probe 同数据同败，pairs≤2 可证数值正确）
    - **跨日波动观察**：今日 C++ 驱动 4.94 / Rust 探针 4.649 / POC 日 4.745——同 chip 不同进程 ±5% 波动，零税+tiling 增益结论稳定
  - **✅ C1-④ 完成（2026-09-19，7de49c7）——单层 language layer 全序 GE 化：对拍 0.00000，1.76× vs eager**。`ge_layer_probe`（apxinf-model 例程）：GEMMA_2B 层（attention 尾 + geglu MLP）全序声明为 GE 静态 OM（PFA(BSH,GQA 8:1,d=256)→Squeeze(axis=0)→proj mm→+x→AddRmsNorm(+zeros)→gate/up 双 mm→GeluV2(tanh)·up→down mm→+res），同输入过 eager aclnn 组合：**m=8 对拍 max_diff=0.00000**（GE 编进同一族 aclnn kernel，逐位一致）；**m=832 bench GE OM 10.28 vs eager 18.06 ms/layer（1.76×）**——外推全模型 ~380ms 验收线量级，与 POC 外推自洽。**bring-up 三大新陷阱（全部取证定罪，已回填 ge-offline-om skill 陷阱表 #8-11）**：① **多输出算子默认输出解析静默失败**——`SetInput(dst_port, src)` 对 AddRmsNorm（y/rstd/x_out）这类多输出 src 边不落图（物化 dump `in0<-<none>`），编译死在断边点且 plog 无 error 行 → 三参 `SetInput(dst, port, src, src_port)`（geb_link_out）；这是全部"norm→matmul 拒绝"的真根因（rstd 死端是红鲱鱼——排查中顺带补了 geb_graph_outputs_idx 索引式图输出绑定）② **PFA 必须 rank-3 desc** [1,m,*]（BSH），2-D 静默拒 ③ **裸 RmsNorm 只有 dynamic 变体**——直出图输出能编译、作中间节点喂静态 matmul 永拒 → 用 **AddRmsNorm**（eager aclnnAddRmsNorm 同名同端口 x1/x2/gamma→y，x2=zeros Data）。Squeeze 桥接 rank-3→2（axis 是 int-list attr 无张量输入）。例程保留 GEB_SUB 1-9 编译冒烟矩阵（逐算子/逐组合二分用）。**开放项**：ApplyRotaryPosEmb 入图编译拒（layout attr/表形状未验，C2 处理；host-rope v1 已闭环不阻塞）。C1 四项全部完成，**进 C2 三段 OM 全量**
  - **C2 全量**：⑤ 三段 OM（vision+embed 27 层 / prefix 18 层 / flow 18 层——flow 10 步用单 OM 执行 10 次，noise 走外部输入每步换绑定）；⑥ OM 缓存按 (shape 签名, 引擎版本) 落盘/加载（首编可能分钟级，必须缓存）；⑦ 全深度 bench → 验收线判定
  - **✅ C2 三段 OM 全量落地（2026-09-19，f0787fa）——编译/运行/缓存全通，bench 不达标**。`ge_model_probe`（apxinf-model 例程，~1900 行）：三段静态 OM（vision 27 层 LayerNormV4+batch PFA / prefix 18 层全序+每层 k/v 输出（末层无 tail，compute_tail 语义）/ flow 18 层单步 OM——styles、prefix k/v、euler 常数全为外部输入），eager 参考对拍 + GEB_SAVE/GEB_LOAD OM 缓存（/data/apxinf/om_cache/{vision,prefix,flow}.om）。**对拍**：flow 全深 **1.6%** ✓；vision/prefix 编译运行通但全深漂移（104%/30%——当时归因 GE 静态 tiling vs eager 运行时 tiling 变体差；**⚠ 次日证伪：实为 eager 参考竞态，修复后 vision 全深 0.1%，见下轮攻坚段与 skill #19/#20**；单算子/组件级全部逐位验证）。**bench @P=832：vision 723ms / prefix 750ms / flow 70.9ms/步 → 全模型 ~2182ms，距 378ms 验收线 5.8×——不达标**，慢源待 msprof op 级定位（嫌疑：TileD/SliceD/Reshape 拷贝类 kernel 低效、LN 辅输出）。**C2 新取证（skill 陷阱 #12-18）**：BroadcastToD 310P kernel 编译崩→TileD；DYNAMIC_INPUT 端口工厂不预建（GetDynamicInputNum=0）→符号直链 AddDynamicInputDesc（端口名 x0/x1 从 0 起）；Reshape 输出绑图输出→desc 动态 [-1]（size 无效、malloc 207001）→图输出只绑静态 desc 节点；AddLayerNorm 在 [768,1152] kernel 级 100× 放大（eager/GE 一致）→LayerNormV4；LayerNormV4 的 mean/rstd 死端会让 y 爆（与 AddRmsNorm 相反）→绑图输出；Reshape→SliceD / mm(rank-3)→下游 组合崩→全链 rank-2+Reshape 桥。**顺带取证**：生产 eager 的 qkv/gate_up take_rows 切分在交织布局下数学错误（flat 切分 ≠ 列切分；此前全部 smoke 只查有限从未暴露——Rust eager 从未做数值金标准对拍），GE 侧 SliceD 通道切分才是对的；修复决策后置（eager 将降级回退路径）。ge_builder FFI 增强：geb_dyn_inputs/geb_dyn_probe/geb_link_idx。**下一步（C3 前）**：msprof 三段 OM 的 task 分布定位慢 kernel（对照 C1 单层 10.28ms 的构成差）；oracle 对拍；ARPE/bias 入图优化。
  - **⚙ C2 性能攻坚第一轮（2026-09-19，见 summary/2026-09-19_c2-perf-attack-round1）——定位完成、未提速、对拍销案**。三轮 msprof（ascend-msprof skill）定律：vision OM ~700ms 中**设备 busy 仅 ~87ms，~600ms 是 81 次/迭代的毫秒级空隙**（形态 `EVENT → ~20ms → MemcopyAsync`，跨流同步后 host 轮询延迟量级）；bmm 20µs/次 vs PFA 323µs。**排除两大嫌疑**：① PFA host 回调（InnerPFA 每次执行 host tiling）——换手工 attention（BatchMatMulV2+SoftmaxV2+headsplit，对拍 0.1%）仍 700ms；② unknown-shape 动态拆分——Const 折叠 shape（OM 221MB→16MB）仍 700ms，且 **flow 带 2683 个 unknown 标记 + PFA + Data shape 却 70.9ms（反例实锤）**。**头号嫌疑（未决）：111 图输出**（LN aux ×108 + 主输出）vs flow 的 1 输出——GEB_NO_AUX 全深隔离实验进程卡死无结论，**下一动作：GEB_DEPTH=2 + GEB_ROUNDS=5 最小 A/B（分钟级）**。**重大修复：eager 参考竞态**（同步 d2h 不等 compute 流 + 闭包中间量 drop 内存复用踩烂，skill #19/#20）——**C2 记录的 vision 104%/prefix 30% 漂移销案，vision 全深 27 层对拍 0.1% 稳定，fp32 oracle 待办撤销**。新工具：GEB_ATTN=manual / GEB_OPT_* / GEB_INIT_OPT_* / geb_add_const_i32 / GEB_DBG / GEB_NO_AUX / GEB_ROUNDS+GEB_PER（最小验证纪律）；BiasAdd 在 310P 注册表存在（未接线，bias 链单 kernel 替代候选）。陷阱回填 skill #19-27。
  - **✅ C2 性能攻坚第二轮（2026-09-19，见 summary/2026-09-19_c2-perf-attack-round2）——根因捕获：切片视图运行时物化，三段修复 5.06×**。第一动作最小 A/B 反向击毙 111 输出假设（noaux 更糟 710ms：死端 mean/rstd 换 LN 动态变体）；上轮 vision_ma profile 重挖出铁证：**82.4 次 MemcopyAsync/执行全在执行流上、每次前 4.5-12ms host 停顿、下个 kernel 恒为切片消费者（te_transpose）**——根因 = SliceD 列切视图触发运行时 D2D 物化（host 慢路径）。PFA 模式同数（27×3=81）反证共同因子是 q/k/v 切片而非 attention 实现；flow 反例解释（unknown 动态路径把切片物化成 kernel，不付此税）。**三个图组织修复**（全 env 门控）：GEB_QKV3=1（qkv 3×mm 独立权重输入）、GEB_ROPEFLAT=1（rope 换 flat 视图行交换，GatherV2D Const 索引）、GEB_ATTN=manual（三段统一手工/GQA/cross attention，scale 折权重）。**全量确认（median/10×30）：vision 723→68.06（0.1%）/ prefix 750→187.75（25.1% ⚠ 随 MLP 宽度单调，假设大 K fp16 累加序差，M3 判）/ flow 70.9→17.51/步（1.6%）→ 全模型 2182→~431ms（5.06×），距 378ms 线 14%**。顺带修 flow eager 段级索引陈旧 bug（const_i32 化后槽位漂移，const 改造后 flow 未重跑所以一直没暴露）。OM 产物 {vision,prefix,flow}_r2.om。陷阱回填 skill #28-29；msprof 顽固导出失败 + sqlite 恢复路径记入 ascend-msprof skill
  - **✅ C2 收尾（2026-09-20，见 summary/2026-09-20_c2-wrapup-nz-weight-tax）——prefix 漂移销案 + NZ 权重税根因 + WCONST 三段 → 全模型 263ms = 378 线 0.70×，C 路线达标进 M3**。① prefix 漂移深度扫描（2/4/8/18 = 6.0/10.9/15.6/25.1%）平滑次线性无跳变 + **layer0 k/v 逐位一致、首块后才分岔** → fp16 大 K 累加序差坐实（probe 侧销案，M3 e2e 判）；② flow 分解 ms(d)=0.06+0.975d（成本全在图内每层）；③ **NZ 权重税根因**：GE 对每个 ND Data mm 权重**每执行**插设备侧 ND→FRACTAL_NZ TransData（flow 54% 总时长 ≈ 531µs/层；torch_npu TransData 100ms 同源，TorchAir 靠权重入图逃税）；④ 单算裁决 GEB_OPTEST=wgt：**Const 入图编译期折叠实证（0.3945→0.2945 ms/mm −25%、OM 烤入权重 8.4MB）**，NZ desc 直入 310P 编译崩（开放项）；⑤ **GEB_WCONST 三段**（binds 全量库存 + data_inputs 映射，eager 索引两模式恒同——depth2 parity 逐位同验证）：vision 68.06→**55.89** / prefix 187.75→**128.44** / flow 17.51→**7.90 ms/步** → **全模型 ~431→263.3ms（8.3× 累计），378ms 验收线 0.70×（比 torch_npu 基线快 31%）**。OM _r3.om 三件套 5.2GB（**per-checkpoint**，ADR-002 缓存 key 更新）+ GEB_LOAD 验证通过。陷阱 #30 回填 skill
  - **C3 判定收尾**：⑧ ≤378ms 或接近 → M3 LIBERO 对标；⑨ 文档/skill 回填（ge-offline-om skill 实战检验）——**已随 C2 收尾达成（263ms = 0.70× 线 + skill #30 回填），C 路线验收闭环**
  - **✅ M3 起跑第一动作（2026-09-20，281d703）——真 checkpoint 权重接入三段 OM（`GEB_CKPT`）**。复用 `Pi05Weights::from_safetensors` host 解析管线（LeRobot 前缀归一、[in,out] 物理转置、**Gemma 1+w scale 折叠**——g1/g2=ones、折叠进 q/k/v/gate/up，probe 图直接消费该约定）→ bf16→f16 → 既有 `wt()` Const 路径烤入 OM。映射面：vision 全 LN/bias 有值（SigLIP 块 + patch/pos/proj）；prefix/flow 的 Gemma 投影无 bias（真权重=zeros）；ada-norm 条件向量（ascl/ash/mscl/msh/fsc/fsh）与 x0/state/pk/pv 保持合成运行时输入（两路同值对拍不受影响）。**真 OM 三件套 `{vision,prefix,flow}_real.om`（字节数与 _r3 逐位同 = 同拓扑），bench 与合成 r3 打平：vision 55.86 / prefix 124.61 / flow 7.89 ms/步（全模型 ~259ms），GEB_LOAD 加载路径验证通过——真权重零延迟代价**。**真权重 parity**：vision 0.1%（同合成）；prefix 深度曲线 **16.8/59.7/100/100%**（d=2/4/8/18；合成时 6.0→25.1%）——layer0 K/V 仍逐位一致、绝对 max_diff 次线性（5.5→36.7）平滑饱和，fp16 累加序机制被真 Gemma outlier 通道放大，非 bug，e2e 判定；flow 37.4%（输出 |max|≈1.1 小分母 + 同机制）。checkpoint 加载 61s/进程（全量 mmap+解析，可后置优化为按段懒加载）。⚠ GEB_LOAD 真 OM 必须配 GEB_CKPT（eager 参考从 binds 取权重）
  - **✅ M3 第二步（2026-09-20，c8ca41b）——三段 OM 接入真实推理链（`GEB_SEG=e2e`，见 summary/2026-09-20_m3-e2e-chain-wiring）**。进程内链式 host 中转：x0 = vision_out ‖ token 查表（embed_prefix 序；state 走离散化进 prompt 不进 prefix）、styles 真值（sinusoidal→time_mlp→style→ascl=1+s0，host f32 每步 h2d 换绑）、pk/pv = prefix 36 输出直连、10 步 flow 换绑（euler 图内）。chip6 bring-up 全通（GE_E2E_PROBE_OK，|x|max 89.9→34.6 单调收敛，actions 有限）；GEB_E2E_GOLDEN safetensors 对拍钩子就位
  - **✅ M3 对拍偏差定位（2026-09-20 晚，c7db8c8——见 summary/2026-09-20_m3-parity-drift-bisect）**。首帧 314% 拆解三层：① rope f16-inv_freq 语义差（torch `.to(f16)` 连 rotary buffer 量化）已修 `rope_angle()` + t200 重烤，kvk_l0 12.6%→0.4%；② 语义/权重/attention/MLP 全部教科书级洗清（0.03-0.14%，golden v2 + 七分析脚本 + probe bisect 工具链）；③ 剩余 = **GE OM 图内数值级**：同链 x0 输入 0.4% vs golden-h1 输入 18.7%（嫌疑：AddRmsNorm f16 方差大动态范围 / WCONST 烤入 / mm cube 累加；另证中间量输出槽被图内复用覆写）。性能零回归：prefix 138.81ms@968 / flow 8.12ms/步 → **稳态 e2e ≈ 275.6ms（378 线 0.73×）**
  - **✅ M3 真根因终结（2026-09-21 凌晨——见 summary/2026-09-21_m3-empty-camera-mask-root-cause）**。前晚"GE OM 图内数值"结论部分推翻：**314% 真根因 = empty_camera 视图语义**——golden 9/10 语义只喂 2 真实视图，empty 走 missing 路径（-1 pad 图进 SigLIP 但 pad_mask=0）：pad_2d 遮蔽 + cumsum 位置塌缩（文本 512..711）；引擎与 golden v2 replay 都把它当一等公民（可见 key + 位置 512..767）。五步裁决（f64 链 18.71% 与 GE 实测全等 → m0_gold 第三世界 35.31% → 图像行画像 → golden_gen 源码 `image_keys[:2]` → theory_check 等价性 0.128%）。**修复 = `GEB_PREFIX_DROP_EMPTY=256`（x0 剔除空视图行 968→712）+ golden v3（frame0_v3，自洽 0.03-0.12%）+ t712 OM**。修复后 x0_vis 0.1% / kvk_l0 0.3% / step0_x1 10.1%；**剩余漂移精确定位到层内 {o_proj mm + res + addrms} 跨度 ~2.8% 执行误差**（m0 0.52% 传播上界仅 0.03%、torch 同链 f16 全程 0.03%——真执行误差；down/act 3.15/3.05% 无罪）。性能再降：prefix 115.62ms@712 / flow 7.64 → **稳态 e2e ≈ 248ms = 378 线 0.66×**
  - **✅ M3 剩余漂移定罪（2026-09-21 上午——见 summary/2026-09-21_ge-addrms-ingraph-corruption）**：attention 链（最小图 m0sq 0.229%/res 0.089%）、gate 宽 N mm（0.057%）、down 大 K mm（0.017%）、MLP 尾组合（0.032%）、权重折叠、GQA 簇、rope、M 对齐、auto-bind、dual-role **16 步全部洗清**；唯一定罪 = **[在图中间量 → AddRmsNorm] 数值污染（11-13%）**——同值作 Data 输入 0.073% 干净（wmm4 决定性实验）、去 addrms 0.036% 干净、五类屏障/端口变体逐位不变、小图 [mm→addrms] 干净、图形状强敏感（net 45.2/dbg 24.7/垫M 24.66/MIN 11.0）⇒ **GE 静态 OM 内存计划重叠活 buffer**（嫌疑；机制待下轮钉死）。最小复现 ~13 算子：`GEB_OPTEST=attn GEB_ATTN_MIN=1`。生产影响面 = prefix 每层 2 处 [add→addrms]×18 + flow（1.6% 同病小 shape）→ kvk_l17 84%/actions 323%
  - **✅ M3 addrms 污染根因终结 + 修复落地（2026-09-21 下午——见 summary/2026-09-21_inplace-addrms-fusion-root-cause）**：上轮"内存计划重叠"的实体 = **`InplaceAddRmsNormFusionPass` 把 AddRmsNorm 融合成原地算子 `InplaceAddRmsNorm`**（310P/910b 注册表同构：outputs 名就叫 x1/x2——结果写回输入 buffer 本体），别名与静态内存计划在读在图中间量时污染；Data 输入因用户 buffer 受保护而免疫（与 16 步证据全兼容）。**修复 = init 级 `ge.fusionSwitchFile` 关该 pass**（官方可关融合规则）：MIN 复现图 11.041%→0.159%、全量 attn 图 45.224%→0.159%。**生产三段重烤（t712fix）**：prefix worst 100%+→**0.8%** 且 124.61→**115.99ms（快 7%）**、flow 37.4%→4.4%（fp16 累加序残余）且 7.89→7.57ms、vision 不变——**修复零代价净赚**。取证新工具：`DUMP_GE_GRAPH=3` 逐 pass 图 dump（发现融合的破局点）、GE 开源仓（本地 D:\compass\ge）选项归属索引（`ge.*` 名 init 级 / atc 短名 build 级；bufferOptimize 合法值 `off_optimize`）。skill ge-offline-om 陷阱 #31 + 工具箱 #6/#7 回填
- **✅ M3 step0 残差 bisect 结案（2026-09-21 下午——见 summary/2026-09-21_m3-flow-step0-residual-bisect）**：三候选输入源两腿正交替入全洗清——PK_GOLD（pk/pv←golden）12.4% 不动、STYLE_GOLD（cond←golden）11.8% 全等基线（cond host-vs-golden 仅 0.0004-0.012 fp16 量化级，te/mlp 语义正确）⇒ **残差 = flow 段执行语义（fp16 全链 vs torch Gemma RMSNorm fp32 上浮）**；abs 视角 step0 0.35 < prefix l17 1.6（rel% 小分母假象），actions ~360% = 小分母 + 10 步轨迹混沌放大（noise 敏感同源）。**parity 追查止于此，裁判移交 LIBERO**。工具：GEB_E2E_PK_GOLD/STYLE_GOLD 旋钮 + golden v3b（cond_s{0..9}）；kvv 对拍曲线 1.1→4.8% 与 k 侧对称
- **✅ M3 真实 e2e 稳态延迟 bench（2026-09-21 晚——见 summary/2026-09-21_m3-e2e-steady-bench）**：**全口径稳态 ≈310.3ms/调用（vision 66.82 + prefix 140.39 + flow 103.08，P50×10，chip6，t712fix）= torch 基线 376ms 的 0.82×（快 18%），延迟验收线"显著优于 378"达成**。口径对齐说明：含全部 host glue（36 路 kv 13MB host 中转 d2h、37×2 styles h2d/步、x d2h），vision +10.7ms aux d2h 属保守计入（生产可免，真实 ≈300ms/0.80×）。styles host 计算 173ms/步（bring-up 定罪的大头）→ 一次性预计算缓存 2.06s/进程（10 步调度固定跨调用不变）。新旋钮 `GEB_E2E_BENCH=<N>` 三段稳态循环。优化潜力排序：pk/pv 设备直连（−15~25ms）> styles h2d 批量/设备化（−10~15ms）> aux 不下载（−10ms）→ ~265-275ms 可达（非阻塞）
- **M3 剩余**：**LIBERO 对标**（9/10 基线，验收 ≥9/10−1pp）——集成工程：真 tokenizer、图像 patch 管线、pyo3/服务链接入 harness、actions 反量化；（不达标才做）RMSNorm fp32 上浮对齐；后置优化：pk/pv 设备直连、styles 设备化、懒加载（100s 加载）
  - **预置风险**：PFA 的 GE IR 注册名/attrs 待验证（C1 第 0 个实验，aclnn 用的 V3，GE 侧注册名可能不同）；~2000 op 大图编译时长/内存未知（POC 19 节点秒级；不可行则按层拆 OM 接力）；ada-norm 先 GE 内置组合（AscendC kernel 入 GE 图走 AscendC-ops-dev skill 的 ge_fullgraph 路线，后置不阻塞）；ACLGraph 路径保留为回退
    - 已完成前置：kernel 面_ops 层（matmul/add/mul/silu/rms_norm/pfa/cat/bias/euler，全部真机对拍）；normal_generator；feature 挂接；M2 构建半
  - 完成后 checkpoint bench → LIBERO 对标（M3）
- [ ] `Backend` trait 最小集：matmul（aclnnMatmul）、rms_norm、silu、add/mul/scale、embedding、rope
- [ ] sdpa（aclnnFusionAttention，310P3 覆盖验证）+ KV cache
- [ ] PI0.5 FP16 executor：CUDA 融合 kernel 先拆基础算子跑通，再热点融合
- [ ] graph capture 等价物（先 no-op + synchronize，后 host 录制回放）
- [ ] PyO3 绑定接入阶段一的引擎抽象缝
- [ ] 验收：random-weights bench → checkpoint bench → LIBERO（对标阶段一基线）

## 技术相异点清单（一个一个过）

| # | 相异点 | CUDA 侧 | NPU 侧候选 | 风险 |
|---|---|---|---|---|
| 1 | GEMM | cuBLAS/CUTLASS（bf16/fp8/w8a8） | aclnnMatmul + FRACTAL_NZ 权重布局 | NZ 转换时机（load 时 vs 首算） |
| 2 | Attention | FMHA/FA | aclnnFusionAttention | 310P3 支持的头数/head_dim 组合待验证 |
| 3 | 融合 kernel | ada_gate_residual_rms_norm / qkv_rope / euler_update 等 | 拆基础算子 → AscendC 自研 | 性能差距主要来源 |
| 4 | 图捕获 | CUDA Graph | **ACLGraph（`aclmdlRI*`）** | **已真芯验证**（2026-09-17：CANN 9.0.1 容器 + 现有宿主驱动，三模式 capture→replay 全绿；8.5.1 runtime 未激活返回 207000——闸门是 CANN 版本非驱动，双容器策略无需停机） |
| 5 | 精度 | BF16/FP8/INT8 | FP16/INT8 | FP16 溢出风险（norm 前 probe） |
| 6 | RoPE | 自研 kernel | aclnnRotaryMul（= npu_rotary_mul） | qkv_rope 融合形态 |
| 7 | 采样/flow steps | device 侧 | 基础算子序列 | 延迟占比小，后置 |

## 里程碑与验收

- M1（阶段一完成）：`build_robot_policy(..., engine="npu-torch")` 可用，bench 出延迟数字，精度对齐 ModelZoo
- M2（阶段二最小）：`apxinf_py` 以 `--features ascend` 构建成功，random-weights bench 在 310P3 跑通
- M3：NPU-native 路径 LIBERO 成功率 ≥ torch_npu 路径 - 1pp，延迟目标对标 378ms 基线（争取显著优于此）
